{
  "name": "interview-review-zoom-app",
  "version": "0.1.0",
  "private": true,
  "description": "Zoom App: visual + audio canary injection for a consented live interview",
  "type": "module",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
