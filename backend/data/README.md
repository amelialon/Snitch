# Local storage (`STORE=local`, default)

The API writes interviews here: `{id}/interview.json` and `{id}/files/*`.

This directory is created automatically if missing. Contents are **not** committed — each developer keeps their own uploads locally.
